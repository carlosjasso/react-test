import React from "react";
import { Layout } from "../layout/default";

export function About() {
    return(
        <Layout>
            <main id="about">
                <h1>Hello from about!</h1>
            </main>
        </Layout>
    );
}